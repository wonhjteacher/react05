import React from 'react';
import './../style/Footer.css';

const Footer = ({title}) => {
    return (
        <footer id="footerArea">
            <h2>{title}</h2>
        </footer>
    );
};

export default Footer;